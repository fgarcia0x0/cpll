#pragma once

#include <token_stream.h>

namespace pll
{

    class ast_parser
    {
    public:
        constexpr ast_parser() = default;
        ast_parser(std::string_view input, const optional<token_stream>& stream)
        {
            
        }
    private:
        
    };

}